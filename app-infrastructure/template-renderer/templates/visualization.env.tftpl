# PROVISIONAL (spec open item 1): the rewrite visualization service takes ONE
# HPDS base URL but BDC splits auth/open HPDS. Pointed at auth-hpds pending the
# team decision on split-URL support in pic-sure-visualization-resource.
HPDS_BASE_URL=http://auth-hpds.${target_stack}.${env_private_dns_name}:8080/PIC-SURE
